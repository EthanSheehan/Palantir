---
phase: 00-foundation-react-migration
plan: 07
type: execute
wave: 6
depends_on: ["00-06"]
files_modified:
  - src/frontend-react/src/theme/palantir.ts
  - palantir.sh
  - src/frontend-react/src/main.tsx
autonomous: false
requirements: [P0-ECHARTS, P0-LAUNCHER]

must_haves:
  truths:
    - "ECharts dark theme registered and available for future chart components"
    - "palantir.sh starts Vite dev server on port 3000 instead of serve.py"
    - "Full React app renders with globe, sidebar, all tabs functional"
  artifacts:
    - path: "src/frontend-react/src/theme/palantir.ts"
      provides: "ECharts dark theme configuration"
      exports: ["PALANTIR_THEME", "registerPalantirTheme"]
    - path: "palantir.sh"
      provides: "Updated launcher using Vite dev server"
  key_links:
    - from: "src/frontend-react/src/theme/palantir.ts"
      to: "src/frontend-react/src/main.tsx"
      via: "theme registration on app init"
      pattern: "registerPalantirTheme"
    - from: "palantir.sh"
      to: "src/frontend-react/"
      via: "npm run dev command"
      pattern: "npm run dev"
---

<objective>
Create ECharts Palantir dark theme, update palantir.sh launcher to use Vite, and perform final integration verification.

Purpose: Complete the build tooling migration (palantir.sh) and set up the charting foundation for future phases. Final checkpoint confirms all components work together.

Output: ECharts theme ready for Phase 1+ charts. palantir.sh launches React frontend. Full system checkpoint.
</objective>

<execution_context>
@/Users/Rocklord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Rocklord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-foundation-react-migration/00-RESEARCH.md
@.planning/phases/00-foundation-react-migration/00-UI-SPEC.md
@.planning/phases/00-foundation-react-migration/00-06-SUMMARY.md

@palantir.sh
@src/frontend-react/src/main.tsx
@src/frontend-react/src/App.tsx
@src/frontend-react/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ECharts Palantir dark theme + update palantir.sh + wire theme into app</name>
  <files>
    src/frontend-react/src/theme/palantir.ts,
    palantir.sh,
    src/frontend-react/src/main.tsx
  </files>
  <read_first>
    palantir.sh,
    src/frontend-react/src/main.tsx,
    src/frontend-react/package.json
  </read_first>
  <action>
**1. Create src/frontend-react/src/theme/palantir.ts:**

ECharts custom dark theme matching Blueprint dark palette. Register as 'palantir' theme name.

```typescript
import * as echarts from 'echarts';

export const PALANTIR_THEME = {
  color: [
    '#2d72d2', '#22c55e', '#ef4444', '#f59e0b', '#a78bfa',
    '#06b6d4', '#ec4899', '#ff6400', '#eab308', '#94a3b8',
  ],
  backgroundColor: '#1c2127',
  textStyle: {
    color: '#e2e8f0',
  },
  title: {
    textStyle: { color: '#e2e8f0' },
    subtextStyle: { color: '#94a3b8' },
  },
  legend: {
    textStyle: { color: '#94a3b8' },
  },
  tooltip: {
    backgroundColor: '#252a31',
    borderColor: '#394b59',
    textStyle: { color: '#e2e8f0' },
  },
  categoryAxis: {
    axisLine: { lineStyle: { color: '#394b59' } },
    axisTick: { lineStyle: { color: '#394b59' } },
    axisLabel: { color: '#94a3b8' },
    splitLine: { lineStyle: { color: '#252a31' } },
  },
  valueAxis: {
    axisLine: { lineStyle: { color: '#394b59' } },
    axisTick: { lineStyle: { color: '#394b59' } },
    axisLabel: { color: '#94a3b8' },
    splitLine: { lineStyle: { color: '#252a31' } },
  },
};

export function registerPalantirTheme() {
  echarts.registerTheme('palantir', PALANTIR_THEME);
}
```

Color palette: Blueprint blue primary, then functional colors from the project. Background matches `#1c2127` (bp5-dark default). Text colors match the slate scale used throughout.

**2. Update src/frontend-react/src/main.tsx:**

Add theme registration call after CSS imports:

```typescript
import { registerPalantirTheme } from './theme/palantir';
registerPalantirTheme();
```

Place this after `FocusStyleManager.onlyShowFocusOnTabs()` and before `ReactDOM.createRoot(...)`.

**3. Update palantir.sh:**

Change step 2 from `serve.py` to Vite dev server. Replace line 100:

Old:
```bash
(cd src/frontend && python3 serve.py 3000) &
```

New:
```bash
(cd src/frontend-react && npm run dev -- --port 3000) &
```

Also update the port kill check — keep port 3000 in the stale process kill loop (already there).

Update the step label from "Cesium dashboard" to "React dashboard":
```bash
echo "[2/3] Starting React dashboard on :3000..."
```

Keep all other parts of palantir.sh unchanged.
  </action>
  <verify>
    <automated>cd /Users/Rocklord/Documents/GitHub/Palantir/src/frontend-react && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/frontend-react/src/theme/palantir.ts exports `PALANTIR_THEME` and `registerPalantirTheme`
    - palantir.ts contains `echarts.registerTheme('palantir'`
    - palantir.ts contains `backgroundColor: '#1c2127'`
    - src/frontend-react/src/main.tsx contains `registerPalantirTheme()`
    - palantir.sh contains `cd src/frontend-react && npm run dev` (not `cd src/frontend && python3 serve.py`)
    - palantir.sh contains `React dashboard` in echo output
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ECharts Palantir theme registered. palantir.sh updated to launch Vite dev server. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final integration verification — full system demo</name>
  <files>palantir.sh</files>
  <action>
Run `./palantir.sh --demo --no-browser` and verify the complete React migration works end-to-end. All prior plans must be functional: globe renders, sidebar tabs work, drone cards show commands, strike board approval flow works, drone cam PIP renders.
  </action>
  <verify>Manual visual verification — see steps below</verify>
  <done>Human confirms all 12 verification checks pass. Full React migration complete.</done>
  <what-built>Complete React migration: Vite build, Zustand store, WebSocket hook, Cesium globe with all entity hooks, Blueprint sidebar with MISSION/ASSETS/ENEMIES tabs, drone cam PIP, assistant widget, strike board, theater selector, grid controls, ECharts theme, updated launcher.</what-built>
  <how-to-verify>
    1. Run `./palantir.sh --demo --no-browser` and wait for "React dashboard on :3000"
    2. Open http://localhost:3000 in browser
    3. Verify: Cesium globe renders with dark tiles and zone grid
    4. Verify: Demo banner "DEMO MODE" appears at top
    5. Verify: Sidebar shows "System Dashboard" with MISSION/ASSETS/ENEMIES tabs
    6. Verify: ASSETS tab shows drone cards with mode tags (IDLE/SEARCH/etc.)
    7. Verify: ENEMIES tab shows target cards as they're detected
    8. Verify: Strike board entries appear and APPROVE/REJECT buttons work
    9. Verify: Click a drone card — card expands, drone cam PIP appears
    10. Verify: Drone cam canvas shows synthetic feed with HUD text
    11. Verify: AI assistant messages appear in the MISSION tab
    12. Verify: Demo autopilot completes kill chain without errors in console
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `./palantir.sh --demo --no-browser` starts without errors
- React app renders at http://localhost:3000
- All Python tests still pass: `./venv/bin/python3 -m pytest src/python/tests/ -x`
- Vite build succeeds: `cd src/frontend-react && npm run build`
</verification>

<success_criteria>
ECharts theme registered. palantir.sh uses Vite. Full demo autopilot runs end-to-end with React frontend: globe renders, sidebar tabs work, drone cards show mode commands, enemy cards show states, strike board approval flow works, drone cam PIP renders, assistant messages appear.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation-react-migration/00-07-SUMMARY.md`
</output>
